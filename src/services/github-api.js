import axios from 'axios';

export const GITHUB_URL = 'https://api.github.com';

const protectMasterBranch = (repositoryName) => {
  //TODO
};

const createIssue = (repositoryName, issueTemplate) => {
  //TODO
};

export default {
  protectMasterBranch,
  createIssue,
};
